{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Schema for a joke",
    "type": "object",
    "properties": {
        "type": {"type": "string"},
        "setup": {"type": "string"},
        "punchline": {"type": "string"},
        "id": {"type": "number"}
    },
    "required": ["type", "setup", "punchline", "id"]
}